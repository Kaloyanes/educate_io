rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }

    match /{document="users"} {
        allow read if: checkAuth()
    }
  }
}

function checkAuth() {
    return request.auth != null
}
